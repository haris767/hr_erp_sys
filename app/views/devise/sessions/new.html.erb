<p class="login-box-msg">Sign in to start your session</p>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="input-group mb-3">
    <%= f.email_field :email, class: "form-control", placeholder: "Email" %>
    <div class="input-group-append">
      <div class="input-group-text">
        <span class="fas fa-envelope"></span>
      </div>
    </div>
  </div>

  <div class="input-group mb-3">
    <%= f.password_field :password, class: "form-control", placeholder: "Password" %>
    <div class="input-group-append">
      <div class="input-group-text">
        <span class="fas fa-lock"></span>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-8">
      <div class="icheck-primary">
        <%= f.check_box :remember_me, id: "remember" %>
        <%= f.label :remember_me, for: "remember" %>
      </div>
    </div>

    <div class="col-4">
      <%= f.submit "Sign In", class: "btn btn-primary btn-block" %>
    </div>
  </div>
<% end %>

<p class="mb-1">
  <%= link_to "I forgot my password", new_password_path(resource_name) %>
</p>
<%# <p class="mb-0"> %>
  <%#= link_to "Create an account", new_registration_path(resource_name), class: "text-center" %>
<%# </p> %>
